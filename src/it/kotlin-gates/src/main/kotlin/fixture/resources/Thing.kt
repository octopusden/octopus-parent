package fixture.resources

// Production code that merely lives in a package called `resources`. It must still be analysed:
// the exclusion is about resource ROOTS, not about any directory that happens to share the name.
// EmptyClassBlock + MatchingDeclarationName, the same pair the test-resources fixture carries.
class Empty {}
