package fixture.control

// The control: an ordinary package. If this is reported and the `resources` package is not,
// the exclusion pattern is matching path segments rather than resource roots.
class Empty {}
